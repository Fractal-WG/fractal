version: v2
modules:
  - path: .
    name: buf.build/fractal-engine/rpc
lint:
  use:
    - STANDARD
  except:
    - PACKAGE_DIRECTORY_MATCH
deps:
  - buf.build/bufbuild/protovalidate

breaking:
  use:
    - FILE
